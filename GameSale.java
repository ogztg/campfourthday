package campFourthDay;

public interface GameSale {
	void display(Game game, User user, Campaign campaign);
	void update(Game game, User user, Campaign campaign);
	void delete(Game game, User user, Campaign campaign);
}
